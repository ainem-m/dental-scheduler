{
  "name": "@dental-scheduler/shared",
  "version": "1.0.0",
  "description": "Shared types and utilities for dental scheduler",
  "main": "types/index.ts",
  "types": "types/index.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "files": [
    "types/**/*",
    "dist/**/*"
  ],
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}